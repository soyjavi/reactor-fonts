{
  "name": "@soyjavi/reactor-fonts",
  "version": "1.0.0",
  "license": "MIT",
  "repository": "https://github.com/soyjavi/reactor-fonts.git",
  "main": "dist/Circular-SP-Book.ttf",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "setup": "rm -rf node_modules && rm -rf yarn.lock && yarn install --force",
    "pipeline": "yarn version --patch",
    "postversion": "git push --set-upstream origin master && git push --tags",
    "publish:release": "yarn pipeline && npm publish"
  }
}
